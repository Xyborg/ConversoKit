PORT=3000
# All travel data in this example is local mock data — no env vars required.
# Swap the in-memory data sources in src/server.ts for real APIs (Amadeus,
# Booking.com, Skyscanner, etc.) when you're ready.
